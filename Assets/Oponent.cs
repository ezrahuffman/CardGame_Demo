using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System.Linq;

// NOTE: THIS CLASS IS FOR SINGLE PLAYER ONLY AND IS NOT YET IMPLEMENTED
public class Oponent : GamePlayer
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    //public void PopulateData()
    //{
    //    EnemyStats enemyStats = FindObjectOfType<EnemyStats>();
    //    enemyStats.health = _healthSystem.currHealth;
        
    //    enemyStats.cards = _hand.GetComponentsInChildren<Card>().ToList();
    //}
}
